import { InterpolateTemplateDirective } from './interpolate-template.directive';

describe('InterpolateTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new InterpolateTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
